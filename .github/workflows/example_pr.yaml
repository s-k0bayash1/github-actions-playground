name: Example pull request

on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  hoge1:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      - name: Print
        run: echo "hoge1"
  
  hoge2:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    if: github.event.pull_request.head.repo.full_name == 'hoge/hoge'
    
    steps:
      - name: Print
        run: echo "hoge2"
